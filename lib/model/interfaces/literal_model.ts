// CyberUiLiteralModel represents the case when a data object
// is used directly as a model without any wrappers, save function,
// getters/setters, etc. It is sometimes used in the recursive case
// when the root model handles all of that logic.
export interface CyberUiLiteralModel {}
