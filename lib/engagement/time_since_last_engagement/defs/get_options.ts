export interface CyberUiGetTimeSinceLastEngagementOptions {
  // The id of the namespace that the item whose time since last engagement is sought resides in
  namespaceId: string;
  // The id of the item whose time since last engagement is sought
  itemId: string;
}
