export interface CyberUiGetTimeSinceLastEngagementOptions {
  // The id of the collection that the item whose time since last engagement is sought resides in
  collectionId: string;
  // The id of the item whose time since last engagement is sought
  itemId: string;
}
